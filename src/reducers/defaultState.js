// default state for the mtg price

export const mtgCardState = {
  name: undefined,
  price: undefined,
};

// default state for errors when uploading pictures

export const uploadPicState = {
  serverError: false,
  pictureUploaded: false,
};
